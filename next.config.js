/** @type {import('next').NextConfig} */
const nextConfig = {
  eslint: {
    ignoreDuringBuilds: true,
  },
  images: { unoptimized: true }, // Keep this if your images don't require optimization
};

module.exports = nextConfig;
